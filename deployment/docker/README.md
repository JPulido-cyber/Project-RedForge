# Docker

The root `Dockerfile` is a production scaffold based on the committed npm
lockfile. It intentionally avoids process managers and orchestration policy.
Promote immutable images and configure health checks in the deployment platform.
