# Vercel

`vercel.json` declares the framework explicitly. Vercel should otherwise use its
native Next.js defaults. Configure `NEXT_PUBLIC_SITE_URL` per environment and
protect production with the repository's required CI checks.
