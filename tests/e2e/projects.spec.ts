import { test } from "@playwright/test";

test("project index links to a project detail page", async () => {
  test.fixme(
    true,
    "Activate when project routes are published from structured content.",
  );
});

test("unknown project slugs render the application 404", async () => {
  test.fixme(true, "Activate with the future project route contract.");
});
