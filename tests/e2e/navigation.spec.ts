import { test } from "@playwright/test";

test("primary navigation reaches every published top-level route", async () => {
  test.fixme(true, "Activate when Phase 3 introduces the primary navigation.");
});

test("mobile menu opens, traps focus, navigates, and closes", async () => {
  test.fixme(true, "Activate when Phase 3 introduces the mobile navigation.");
});
