# Icons

Icons are dependency-free React SVG components grouped by `navigation`, `status`,
and `system`. Import all icons from `@/components/icons`; do not inline or copy SVG
markup into feature components.
