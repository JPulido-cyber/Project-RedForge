# Components

- `ui`: reusable, product-agnostic primitives
- Feature folders: composed RedForge interface sections grouped by domain

Prefer Server Components. Add a client boundary only at the smallest interactive
component that needs it.
