# Mock data

Feature-owned fixtures may be placed here when a feature needs deterministic local
or test data. Mocks must implement the same typed boundary as the future service
and must never silently run in production.
